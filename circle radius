#include <iostream>
using namespace std;

class Circle {
public:
    int r;

    Circle(int x) {
        r = x;
    }

    float area() {
        return 3.14 * r * r;
    }
};

int main() {
    Circle c1(5);
    cout << "Area = " << c1.area();
    return 0;
}
